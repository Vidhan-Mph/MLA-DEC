spring.application.name=SpringBootRestAPI_JPA
server.port=1342

#ACTUATOR

management.endpoints.web.base-path=/actuator
management.endpoints.web.exposure.include=*

#management.endpoints.web.exposure.include=health,info,beans,env,hello,metrics

management.endpoint.health.show-details=always
#database configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/spring_jdbc
spring.datasource.username=root
spring.datasource.password=root@39
 
#hibernate|JPA
 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

#hakri

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

# logging
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.level.org.hibernate.SQL=debug
#logging.level.org.hibernate.type.descriptor.sql=trace
logging.level.=info
#debug=true 
#trace=true 
logging.file.path=logs/
logging.file.name=logs/application.logs